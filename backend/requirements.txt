# ── Web Framework ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Database (MongoDB) ─────────────────────────────────────
motor==3.4.0              # Async MongoDB driver for FastAPI
beanie==1.26.0            # MongoDB ODM (built on Motor)
pymongo<4.7.0             # Compatible PyMongo version for Motor 3.4.0

# ── Auth ───────────────────────────────────────────────────
python-jose[cryptography]==3.3.0   # JWT creation & verification
passlib[bcrypt]==1.7.4             # Password hashing

# ── Config & Validation ────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.3.0
python-multipart==0.0.9    # For file uploads (resume PDF)

# ── AI / LLM / LangChain ───────────────────────────────────
langchain==0.2.5
langchain-community==0.2.5
langchain-core>=0.2.9
langchain-groq>=0.1.5      # Groq high-speed LLM integration
groq>=0.9.0                # Official Groq Python SDK

# ── Live Job Scraping (Agent 2) ────────────────────────────
python-jobspy>=1.1.55      # Multi-source scraper (Indeed, LinkedIn, Google Jobs)
pandas>=2.0.0              # Data manipulation for job results

# ── Resume Parsing (Agent 1) ───────────────────────────────
PyMuPDF==1.24.5            # PDF text extraction (fitz)

# ── HTTP Client & Utilities ────────────────────────────────
httpx==0.27.0              # Async HTTP client
python-dotenv==1.0.1       # Load .env files
